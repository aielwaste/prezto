#
# suffix aliases for macos
#
# Authors:
#   Andrew Bell <andrewbell8@gmail.com>
#


# script runners
alias -s pl=perl
alias -s py=python3

alias -s git='git clone --recursive'
alias -s pdf=preview
alias -s md=macdown
alias -s markdown=macdown
alias -s mdown=macdown

# Sublime Text
alias -s sublime-settings='subl -n -w'
alias -s sublime-menu='subl -n -w'
alias -s sublime-commands='subl -n -w'
alias -s sublime-build='subl -n -w'
alias -s sublime-snippet='subl -n -w'
alias -s sublime-macro='subl -n -w'
